[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bitrix24-mcp"
version = "0.1.0"
description = "MCP server for Bitrix24 tasks via incoming webhook"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
bitrix24-mcp = "server:main"

[tool.hatch.build.targets.wheel]
packages = ["."]
